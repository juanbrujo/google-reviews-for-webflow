# Copy to .env for local Netlify dev
# Note: Review limit is controlled by data-max attribute in the widget, not here
GOOGLE_PLACES_API_KEY=
PLACE_ID=ChIJN1t_tDeuEmsRUsoyG83frY4
REVIEWS_LANGUAGE=es

# PLACE_ID is optional - used as fallback when data-placeid attribute is not set
# You can override per-widget by adding data-placeid="YOUR_PLACE_ID" to the HTML div
